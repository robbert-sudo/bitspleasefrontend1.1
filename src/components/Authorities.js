import React from "react";

function Authorities ({authorities}) {

    return (
      <div className="authorities">
          <h1>{authorities.authority}</h1>
      </div>
    );
}

export default Authorities;